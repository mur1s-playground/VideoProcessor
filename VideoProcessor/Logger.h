#pragma once

#include <string>

using namespace std;

void logger(string text);
void logger(int i);
void logger(unsigned int i);
void logger(float f);
void logger(long l);
void logger(unsigned long long ul);
